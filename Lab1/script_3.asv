clear;
close all;

X = -1:0.01: 1;
u = 0;
b = 2;

F = @(x) ((0.5 * (eulergamma .^ ((x-u)/b))) .* (x <= u)) + ((1 - 0.5 * (eulergamma .^ (-1 * ((x-u)/b)))) .* (x > u));
F1 = @(x) -b * log(-2*(x-1));

Y2 = F(X);
Y3 = F1(X);

hold on;
grid on;
plot(X, Y2);
plot(X, Y3);

% figure();
% 
% histogram(Y3)
